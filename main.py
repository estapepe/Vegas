import random


class Die:
    def __init__(self, cardinality, color):
        self.color = color
        self.upper_surface = random.randint(1, cardinality)

current_game =
current_game.play()

current_set =
current_set.play()

replicate_cardinality = 10
alpha_experiment = VegasExperiment(replicate_cardinality, VegasExperiment.ALPHA)
experiments = [alpha_experiment]
current_experiment_set = VegasExperimentSet()
current_experiment_set.execute()